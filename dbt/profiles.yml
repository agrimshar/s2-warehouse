s2_warehouse:
  target: dev
  outputs:
    dev:
      type: duckdb
      path: ../data/warehouse.duckdb
      extensions: [delta, httpfs]
      secrets:
        - type: s3
          provider: credential_chain
          region: us-west-2
      threads: 4