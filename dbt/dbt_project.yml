name: s2_warehouse
version: "1.0"
profile: s2_warehouse
model-paths: ["models"]
test-paths: ["tests"]
vars:
  silver_path: "../data/delta/silver"
  s3_bucket: "s2-warehouse-agrim-2026"
  metrics_path: "../data/metrics/stage_runs.jsonl"
models:
  s2_warehouse:
    staging:
      +materialized: table
    marts:
      +materialized: table
    ops:
      +materialized: table